import React from 'react';

const Portfolio = () => {
    return (
        <section id="portfolio" className="mb-8">
            <h2 className="text-3xl font-bold">Portfolio</h2>
            <p>Check out my featured projects.</p>
            {/* Adicione os itens do portfólio aqui */}
        </section>
    );
};

export default Portfolio;
